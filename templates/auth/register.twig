{% extends "auth/base.twig" %}

{% block title %}Crear compte{% endblock %}

{% block content %}
<div class="auth-form registre-form">
  <h1>Crear compte</h1>

  <img src="assets/icons/times.svg" class="back-icon" alt="Tornar" onclick="window.location.href = '/'" title="Tornar">

  {% if errors %}
    <div class="missatge-error">
      <ul>
        {% for error in errors %}
          <li>{{ error }}</li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}

  <form action="/register" method="POST">
    <label for="alies-register-input">Nom d'usuari</label>
    <div class="input-container">
      <input type="text" name="nickname" id="alies-register-input" required autocomplete="off" {% if nickname %}value="{{ nickname }}"{% endif %}>
      <i class="fa-solid fa-user"></i>
      <span id="alies-register-status" class="alies-register-status-icon"></span>
      <span id="alias-register-status-msg" class="alias-status-msg"></span>
    </div>

    <label for="email">Correu electrònic</label>
    <div class="input-container">
      <input type="email" name="email" id="email" required autocomplete="off" {% if email %}value="{{ email }}"{% endif %}>
      <i class="fa-solid fa-at"></i>
    </div>

    <label for="password">Contrasenya</label>
    <div class="input-container">
      <input type="password" name="password" id="password" minlength="8" required>
      <i class="fa-solid fa-lock toggle-password" data-target="password" title="Mostrar contrasenya"></i>
    </div>

    <label for="confirm-password">Repeteix contrasenya</label>
    <div class="input-container">
      <input type="password" name="confirm-password" id="confirm-password"  minlength="8" required>
      <i class="fa-solid fa-lock toggle-password" data-target="confirm-password" title="Mostrar contrasenya"></i>
    </div>

    <button type="submit" class="signup">Registrar-se</button>
  </form>

  <!-- Enllaç per a aquells usuaris que ja tenen un compte i volen iniciar sessió. -->
  <p>Ja tens compte? <a href="login">Inicia sessió</a></p>
</div>{% endblock %}